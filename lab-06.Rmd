---
title: "Lab 06 - Sad plots"
author: "Marcus Minko"
date: "02-16-2022"
output: github_document
---

### Load packages and data

```{r load-packages, message = FALSE}
library(tidyverse) 
library(dsbox) 

staff <- read_csv("data/instructional-staff.csv")

staff_long <- staff %>%
  pivot_longer(cols = -faculty_type, names_to = "year") %>%
  mutate(value = as.numeric(value))
```

### Exercise 1

```{r ggplot-line}
staff_long %>%
  ggplot(aes(x = year, y = value, color = faculty_type)) +
  geom_line()

ggplot(aes(x = year, y = value, color = faculty_type, group = faculty_type)) +
      labs(title = "proportion of faculty type", subtitle = "1975-2011", y = "percentage of faculty") +
     geom_line()


```
### Exercise 2

Remove this text, and add your answer for Exercise 1 here.
Add code chunks as needed.
Don't forget to label your code chunk.
Do not use spaces in code chunk labels.

### Exercise 3

...

Add exercise headings as needed.
